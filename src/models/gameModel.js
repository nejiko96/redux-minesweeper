export const handleLeftMouseDown = (state, i, j) => {
  console.log(`handleLeftMouseDown(${i}, ${j})`);
  return state;
};

export const handleRightMouseDown = (state, i, j) => state;

export const handleBothMouseDown = (state, i, j) => state;

export const handleLeftMouseUp = (state, i, j) => {
  console.log(`handleLeftMouseUp(${i}, ${j})`);
  return state;
}

export const handleRightMouseUp = (state, i, j) => state;

export const handleBothMouseUp = (state, i, j) => state;

export const handleLeftMouseOver = (state, i, j) => state;

export const handleRightMouseOver = (state, i, j) => state;

export const handleBothMouseOver = (state, i, j) => state;

export const handleLeftMouseOut = (state, i, j) => state;

export const handleRightMouseOut = (state, i, j) => state;

export const handleBothMouseOut = (state, i, j) => state;

